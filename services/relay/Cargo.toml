[package]
name = "ztplanet-relay"
version = "1.0.0"
edition = "2021"
license = "MIT"
description = "Hardened ZeroTier TCP fallback relay"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true

